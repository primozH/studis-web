'use strict';

describe('myApp.prijava module', function() {

  beforeEach(module('myApp.prijava'));

  describe('prijava controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var prijavaCtrl = $controller('PrijavaCtrl');
      expect(prijavaCtrl).toBeDefined();
    }));

  });
});